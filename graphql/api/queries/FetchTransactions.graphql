query FetchTransactions {
  transactions(order_by: {trade_date: desc}) {
    id
    account_id
    trade_date
    symbol
    quantity
    price
    fee
    asset_type
    action
  }
  accounts {
    label: name
    value: id
  }
}
