query FetchTransactions {
  transactions(order_by: {trade_date: desc}) {
    id
    account_id
    trade_date
    symbol
    quantity
    action
    price
    fee
    strategy
    asset_type
  }
  accounts {
    label: name
    value: id
  }
}
