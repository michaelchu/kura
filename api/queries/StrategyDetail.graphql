query StrategyDetail($root: String!, $strategy: String!) {
  strategyDetail(root: $root, strategy: $strategy) {
    trading_account_name
    action
    asset_type
    fee
    quantity
    price
    strategy
    symbol
    total_cost
    trade_date
  }
}
