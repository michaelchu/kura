query GoalsAndProgress {
  goals_and_progress(
    order_by: { period: desc }
    where: { interval: { _eq: "MONTHLY" } }
  ) {
    user_id
    strategy
    root
    period
    goal
    interval
    current
  }
}
